(()=> {
    let prices = [1,2,3,4];
    // prices.push(""); //cant insert a string

    let list: number[] = [3 , 5]

    let otherList = [1,2, "hola", true];
    let otherLuist2: (number | string | boolean)[];

})();